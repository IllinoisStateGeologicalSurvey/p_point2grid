// let the compiler know which libraries we have available
#cmakedefine CURL_FOUND
#cmakedefine HAVE_GDAL

// Boost.Iostreams prior to 1.44.0 requires different parameters
// This flag tells the parameter initializaiton code knows what to do.
#cmakedefine OLD_BOOST_IOSTREAMS


#define P2G_VERSION_MAJOR ${PP2G_VERSION_MAJOR}
#define P2G_VERSION_MINOR ${PP2G_VERSION_MINOR} 
#define P2G_VERSION_PATCH ${PP2G_VERSION_PATCH} 

#define P2G_VERSION_STRING ${PP2G_VERSION_STRING}
